# 实施计划

[方案] 沿用原有 KMeans 有界候选分组，把每组排名分发给 Spark；候选集合仍在 driver 有界准备，最终结果收集落 Mongo，不能宣称 driver 无内存限制。
[方案] 评价使用标准库命令行工具，输入 JSON 人工标签，输出可保存的 JSON 指标。真实标注需要项目成员完成。
[方案] 文献最新批次读取改为分块精确键查询，避免一文档一次网络往返，保留历史批次过滤契约。
宪法检查：修复核心相似度边界；保持 Mongo/规模/人工评价要求，未改变评分标准。
