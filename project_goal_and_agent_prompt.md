# 课程项目 Goal 与下一阶段 Agent Prompt

## 一、项目 Goal

在三周课程周期内，由 `twj` 和 `cmy` 共同完成一个可公开汇报、可运行、可答辩、证据可追溯的课程项目：

> 基于 Spark 与 MongoDB 的多源技术内容知识挖掘与个性化发现 Web 系统。

项目应基于已核验的数据源，形成一条完整链路：

```text
多源技术文档 → Spark 数据处理与挖掘 → MongoDB 文档管理
→ Web API → Web 交互与可视化 → 个性化知识发现
```

项目优先争取使用真实分布式 Spark 集群；如果课程环境或权限不允许，则采用本机 Spark local 模式作为明确的降级方案，并如实记录其与分布式方案的差异。不能把 local 模式描述成真实分布式集群。

项目先保证课程基线，再争取提升和惊喜：

### 基线版本：必须完成

- 可核验的多源文档数据，最终目标不少于 1 万条；
- Spark 实际参与文本清洗、特征提取或挖掘；
- MongoDB 保存原始文档、处理结果和用户知识记录；
- 一个可运行的后端 API；
- 一个可运行的 Web 页面；
- 基于用户已有知识体系的个性化新知识发现；
- 数据规模、运行日志、API 输出、页面截图和 Git 提交等证据；
- 需求规约、设计规约、提示词记录、项目报告和演示材料。

### 提升版本：基线稳定后实施

- 多来源的主题关联和跨来源推荐；
- 新知识、已知知识、深化知识和冲突知识的可解释展示；
- 关键词、主题或推荐结果的质量评价；
- B 站技术视频或 RSS 博客的稳定接入。

### 惊喜版本：只在不影响基线时实施

- B 站或 RSS 的增量更新；
- 用户反馈驱动的知识库校准；
- 工程问答与学术论文之间的跨来源知识扩展；
- 更丰富的可视化或知识发现解释。

惊喜功能不是项目完成的前提。任何高风险来源、复杂架构或未验证功能，都必须允许被放弃。

## 二、已知项目背景

- 现有仓库为 `/home/lengjing1236/knowpipe/`；
- 现有 Knowpipe 是 Python 本地知识精炼 CLI；
- 已有文本摄入、分块、TF-IDF 候选召回、知识卡、`known/refine/contradict` 差分和反馈闭环；
- 当前没有真实 Spark、MongoDB 和 Web 的完整课程链路；
- 运行目标是本机，但真实分布式 Spark 在评分上优于单机，应优先调查是否可用；
- 数据源候选包括 Stack Exchange、arXiv、B 站和 RSS；
- 当前较稳妥的数据组合是 Stack Exchange 作为主规模数据源，arXiv 作为合规的补充数据源，B 站和 RSS 作为可选特色来源；
- 个性化定义为：根据用户已经积累的个人知识体系，过滤已掌握内容，突出新知识、深化内容和可能存在冲突的内容；
- 团队成员只有 `twj` 和 `cmy`；
- SDD 使用 GitHub Spec Kit；
- 课程资料要求提交需求规约、设计规约、全部 AI 提示词、Git 历史、项目报告、演示视频和分工表。

## 三、资料的权威层级

请区分以下材料的性质：

1. 两份课程 PDF：课程约束、评分标准和参考技术，不是项目已经完成的事实；
2. `README.md`、`DESIGN.md`：现有仓库的功能和设计证据；
3. `audit_report.md`、`feasibility_report.md`、`public_dataset_research_report.md`：项目调研和分析结果，其中仍可能包含待确认的推断；
4. 用户在当前对话中明确确认的事项：项目决策依据；
5. 尚未验证的环境、数据规模、许可证和教师意见：必须标记为待确认，不能当作事实。

## 四、交给下一阶段 Agent 的 Prompt

```text
任务：为课程项目生成可用于公开选题汇报的“选题调研、实施方案与三周计划”。

请先阅读以下材料：

- `README.md`
- `DESIGN.md`
- `audit_report.md`
- `feasibility_report.md`
- `public_dataset_research_report.md`
- `大数据综合实践课程内容说明.pdf`
- `大数据综合实践课程参考资料.pdf`

项目选题：

基于 Spark 与 MongoDB 的多源技术内容知识挖掘与个性化发现 Web 系统。

团队成员：

- twj
- cmy

已确认的项目方向：

- Stack Exchange 作为主要规模数据源；
- arXiv 元数据作为稳定的补充来源；
- B 站技术视频转写和 RSS 技术博客作为可选特色来源，不承担核心规模要求；
- 个性化发现基于用户已有个人知识体系，过滤已掌握内容，保留新知识、深化内容和冲突内容；
- 项目目标是在本机运行；
- 课程评分中真实分布式 Spark 集群优于单机，因此必须优先调查真实集群是否可用；
- 如果没有集群，保留本机 Spark local 模式降级方案，并明确说明评分和答辩影响；
- MongoDB 和 Web 可以使用本机实例，但必须记录运行条件和启动方式；
- SDD 使用 GitHub Spec Kit。

课程硬性约束：

- 文档类数据最终目标不少于 1 万条；
- 必须覆盖数据处理、数据管理与挖掘、后端业务和 Web 交互四层；
- Spark 必须参与真实的数据处理或挖掘；
- MongoDB 必须保存真实文档和处理结果；
- 不能把 LLM 摘要直接当作核心数据挖掘成果；
- 必须保留需求规约、设计规约、完整 AI 提示词、Git 历史、运行证据和演示材料；
- 两位成员都必须能够解释自己负责模块的设计决策。

本轮权限和边界：

- 可以阅读仓库、课程资料和已有调研报告；
- 可以进行不修改文件和系统状态的环境检查；
- 不要安装依赖；
- 不要下载大型数据集；
- 不要修改现有代码；
- 不要创建完整项目结构；
- 不要开始实现 Spark、MongoDB、Web 或前端功能；
- 本轮只产出汇报材料、方案、计划和待确认问题。

决策原则：

1. 优先选择许可证清晰、规模可证明、获取稳定、容易答辩的数据组合；
2. 先保证基线版本，再安排提升和惊喜功能；
3. 主链路不能依赖许可证、规模或获取方式尚未确认的 B 站/RSS 数据；
4. 真实分布式 Spark 是优先方案，Spark local 是明确的备用方案；
5. 所有结论必须区分“已验证事实”“项目假设”和“待教师或成员确认”；
6. 不要把平台名称当作数据集，不要把公开可见当作许可证授权；
7. 不要为了显得复杂而添加微服务、复杂权限或知识图谱等非必要内容；
8. 每项计划都必须有完成证据、负责人、前置依赖和失败后的降级方案。

请按以下结构输出：

## 1. 公开汇报用的选题结论

说明项目要解决的问题、目标用户、数据来源、核心挖掘方法、个性化机制和课程价值。

## 2. 最终数据源建议

选择一个主数据源和一个稳定补充数据源，并说明 B 站/RSS 是否进入核心范围。
对每个来源给出规模证据、许可证边界、获取方式、字段结构、是否计入 1 万条和失败替代方案。

## 3. 课程要求映射

建立“课程要求 → 项目功能 → 技术组件 → 可提交证据 → 当前状态”对照表。
至少覆盖 Spark、MongoDB、数据挖掘、个性化发现、Web、1 万条数据、SDD、Git 和演示材料。

## 4. 分布式与本机运行方案

分别说明：

- 真实分布式 Spark 集群方案：环境、提交方式、运行证据、评分收益和风险；
- 本机 Spark local 方案：运行方式、局限、评分影响和降级条件；
- 本机 MongoDB 与 Web 的运行方式；
- 最终推荐的主方案和备用方案。

不能把尚未获得的集群、MongoDB 或数据集写成已存在。

## 5. 最小实施方案

描述一条最小完整链路：

多源文档 → Spark → MongoDB → Web API → 页面 → 个性化结果。

说明现有 Knowpipe 哪些能力可以复用，哪些必须重新实现，以及 LLM 只应承担什么辅助职责。

## 6. 基线、提升与惊喜范围

分别列出功能、前置条件、验收证据和放弃条件。
惊喜功能不得影响基线版本。

## 7. 三周实施计划

按周和阶段列出任务、负责人、输入依赖、完成证据、风险和降级方案。
计划必须优先安排环境确认、数据获取和规模验证，再安排最小垂直切片、扩展到 1 万条、Web 联调、评估、SDD 和演示准备。

## 8. twj 与 cmy 初版分工表

提供“成员 → 模块/层次 → 具体任务 → 输入依赖 → 交付证据 → 预计占比 → 备份负责人”的表格。
分工必须平衡，并确保两人都能解释自己的设计决策。

## 9. 风险与决策闸门

列出数据许可证、数据规模、Spark 集群、MongoDB、个性化评价和 B 站/RSS 获取等高风险事项。
为每项指定最晚确认时间、验证方法、失败替代方案和是否触发范围缩减。

## 10. 公开汇报结构

给出适合 8～10 分钟汇报的结构：背景问题、数据来源、挖掘方法、个性化机制、四层架构、实施计划、风险与备选方案、预期成果。

最后列出最多 5 个仍需教师或项目成员确认的问题。

完成上述输出后停止，等待人工审阅。
```

## 五、下一阶段的验收标准

下一阶段 Agent 的输出只有满足以下条件，才算完成：

- 有一个明确的主数据源和补充数据源；
- 说明了真实分布式和本机降级两条路径；
- 能将课程评分点映射到项目功能和证据；
- 形成 `twj`、`cmy` 两人的初版分工表；
- 三周计划包含依赖、验证点和降级方案；
- 没有把未确认事项伪装成事实；
- 没有提前生成代码或复杂架构；
- 结论可以直接用于选题确认汇报。

