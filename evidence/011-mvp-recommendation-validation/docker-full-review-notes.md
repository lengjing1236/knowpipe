# Docker tmpfs 三场景独立原文审核

审核者：`/root/mvp_language`（代理原文审核，不是人类标注或学习效果实证）。日期：2026-09-25。

本次仅审核冻结后保留场景 `docker_tmpfs_cold`、`docker_tmpfs_partial`、`docker_tmpfs_covered`。未修改代码、模型、门槛、正式 review；未运行模型或 Spark。判断基于五篇冻结正文，不根据运行状态推断推荐正确与否。

目标原文：**理解 Docker tmpfs 临时文件的内存限制，以及是否一定不会写入磁盘**。分别核对 tmpfs 是否占用容器限额、挂载大小是否增加可用内存、swap 是否允许落盘；一般 Docker 内存知识与具体故障案例要区分。

## 审核版本

运行中的 `current-v6-full.json` 仍在追加其他场景，本文件不宣称最终全文件 SHA。下列 SHA-256 绑定各自完整 case 对象，计算形式为 `json.dumps(case, ensure_ascii=False, sort_keys=True, separators=(',', ':')).encode('utf-8')`：

| case | SHA-256 |
|---|---|
| docker_tmpfs_cold | `e1c5ba9517d618cafc3f2ad5293564bbf2055656026c32284cc4192d028c1b34` |
| docker_tmpfs_partial | `577e91385580330f2b94f6d26ff534bd4cbedb760bd019b87b7af73e52333c9c` |
| docker_tmpfs_covered | `4b105d8b01f627baa30f3c3041393c83093e9151f4a1a92ee891f437ca1cdd04` |

同一规范化规则下，当前 `signature` 对象摘要为 `f6269207efe9d68a50192810794d8d7f5a11c1589a6631a8d6d423d61c5699ac`。这是运行配置签名，不是完整结果文件摘要。写作前复核三个 case 未发生改变。

## 原文定位

语料为 `state/feature009/corpus-expanded/documents.jsonl`。所有区间均指 `body_text` Unicode 码点的左闭右开 `[start,end)`。逐篇阅读五份正文，并核对结果中 111 处正文引用切片，全部一致。

| 简称 | 原始 doc_key（source:doc_id） | 结果中的 doc_key |
|---|---|---|
| V | `docker_docs:unversioned/engine/storage/volumes` | `3a32a9266e13f99f2e286123bb66894a360fcba5c1f3966bd4fc727bf8722844` |
| T | `docker_docs:unversioned/engine/storage/tmpfs` | `56302c7c09b8197ce47109426883a7e8de85dcf04f29386577d5263ffae6572f` |
| R | `docker_docs:unversioned/engine/containers/resource_constraints` | `bb63df0da4666c25de61177369a1dddaa76889fbf46047e553e8abd74e1b567a` |
| S | `stackexchange:serverfault-1161140` | `ebdd222ca4b98402d52522f8a166e4e8aec06f8aa47644f0bd630f9cbbca66f2` |
| K | `stackexchange:serverfault-1190253` | `a1e48c6530f9257edde38c3a5772f1972e2771226960c7f3aeefd6b6eaee2b9d` |

partial 已读 V；covered 已读 V+T。

## 推荐正文与目标的关系

| 正文及出现位置 | 判断 | 原文依据与边界 |
|---|---|---|
| T，cold #1、partial #1 | **完整且直接回答目标** | `[629,991)` 明确 tmpfs 计入容器 cgroup 内存、较大的 tmpfs-size 不额外增加 RAM、仍可能 OOM；`[1297,1462)` 明确可能写入 swap 文件；`[2216,2326)` 再次说明 size 不提高容器限额。 |
| R，cold #3、partial #3、covered #2 | **对目标子问题有直接补充价值，不能独立替代 T** | `[2730,3025)` 区分硬/软限额，`[3289,4195)` 解释 memory 与 reservation；`[4735,6612)` 解释 memory-swap 的条件、总额度与禁止 swap 配置。这些可以帮助理解容器内存限制和落盘控制，但正文没有直接论证 tmpfs 的内存计账方式。covered 已读 T 后，R 的具体限制类型与配置仍可能构成有效扩展。 |
| S，cold #2、partial #2、covered #1 | **只有一般相关性，不直接回答 tmpfs 问题** | `[95,597)` 是整台 Digital Ocean 主机卡顿；`[620,1699)` 是一般 swap 配置建议；被选中的 `[1974,2100)` 仅说 Docker 资源超限可能出问题。没有 tmpfs、挂载大小、临时文件计入限额的关系，也未解释 tmpfs 是否会落盘。 |
| K，covered #3 | **相关故障背景，不直接回答 tmpfs 问题** | `[255,649)` 是 Kubernetes 中 .NET 文件复制的 page cache/OOM；`[1138,1377)` 是提问者本机 WSL2 Docker 的观测。后续 `[2940,6309)` 讨论诊断、Memory QoS 和调整配额，未涉及 tmpfs。普通文件页缓存与 tmpfs 临时文件不能只因都涉及内存就视为同一学习问题。 |

因此 cold/partial 各有 **1 篇完整主答案、1 篇有价值的子问题补充、1 篇弱相关材料**；covered 中 R 仍有真实扩展价值，S/K 不足以支持当前目标的新内容。若计量口径允许直接帮助某个子问题，正文目标相关数为 cold 2/3、partial 2/3、covered 1/3；若口径要求单篇完整回答全部 tmpfs 关系，则只有 T。此处保留区别，避免把相关支持和完整答案混为一类。

另外，cold 选出的 T `goal_evidence` 是 `[424,529)` 的“临时、只在主机内存”概括，没有同时展示后文 swap 例外；**完整正文是正确材料，不等于当前预览足以回答甚至不会误导目标**。

## partial：七条 additional_evidence

### T [0] `[424,529)`：临时、在主机内存中

- V `[1995,2227)` 已明确 tmpfs 用于非持久状态、避免永久存储；**“临时/非持久”已经覆盖**。V 没有明说 tmpfs 在主机内存中，所以整句包含一个未显式覆盖的介质细节，不能整体标成逐字重复。
- 有限的基础解释价值，但不能把它当作新的内存限制或 swap 边界。尤其“only persisted in the host memory”必须结合 T `[1297,1462)` 的 swap 例外理解，不能独立推导“绝不写磁盘”。
- 所选历史 V `[301,632)` 是 volume 的主机目录与 bind mount 管理方式，没选中更直接的已读 tmpfs 概括。结论：**部分已覆盖、部分新增背景；不支持无条件不落盘结论**。

### T [1] `[1363,1462)`：临时数据可能通过 swap 写入文件系统

- V 全文没有解释 tmpfs 的 swap 落盘例外；相反 V `[1995,2227)` 的非持久性概括需要这个限定。
- **未被历史覆盖，直接回答目标，有效新增**。候选正文上一句 `[1297,1362)` 给出 Docker tmpfs 与 Linux tmpfs 的关系，能确定 “As such” 的指代。
- 当前匹配历史 V `[655,938)` 只是 volume 持久化总论，比较配对不精确，但通过 V 全文核对，新增结论本身成立；不能因配对弱就否认真实新增。

### T [2] `[2216,2279)`：tmpfs 数据计入容器内存限制

- V `[1995,2227)` 只讲非持久性与性能，没有内存限额计账。**未被历史覆盖，直接回答目标，有效新增**。
- 被展示的这一句没有包含紧邻 `[2280,2326)` 的“大 size 不提高限额”，完整正文 `[629,991)` 和 `[2216,2326)` 才完整说明边界。不能把单句对齐成功等同于已展示全部内存关系。

### S [0] `[1974,2100)`：容器有 CPU/内存限制，超限可能有问题

- V 没有讲 CPU/内存限额，因此不是已被 V 全文明确覆盖的句子。
- 但这只是通用提醒，**没有说明 tmpfs、size、swap 的任一关系**。和 V `[655,938)` 的 volume 持久化比较，也不能形成当前目标的具体差异。
- 结论：新背景不等于当前目标的有效补充，不计为两个核心新增事实之一。

### R [0] `[3840,4006)`：memory-reservation 是低于 memory 的软限额

- V 全文没有软限额、reservation 或低内存/争用触发条件。**确为未覆盖细节**。
- **对“内存限制”子问题有补充价值**：帮助区分硬上限与软限制；但不说明 tmpfs-size 和 cgroup 的关系，不能替代 T 的核心说明。
- 对照 V `[24311,24414)` 的 volume 删除后仍持久不对应这一命题。此处认可正文知识的新增与目标子问题价值，不认可该对照已充分解释个人差异。

### R [1] `[4102,4195)`：软限制不保证不被超过

- V 没有覆盖软限制规则。**确为新增，与 R [0] 连读时，对内存限制子问题有价值**。
- “it/the limit” 指同一表项 `[3840,4195)` 的 `--memory-reservation`，**不是 tmpfs-size，也不是硬性的 --memory**。若脱离前一条展示，无法安全地当作任意内存上限均可超出的结论。
- 对照 V `[2600,2845)` 的 volume 生命周期无助于补足指代；本判断依靠 R 完整表项，而不是模型的关系标签。

### R [2] `[4815,4959)`：RAM 用尽后 swap 可将容器内存写到磁盘

- V 没有 swap 机制说明。**未覆盖，对是否可能落盘有直接支持价值**；它讲容器一般 swap，与 T 的 tmpfs 特例结合才完整。
- 与同一推荐列表 T [1] 已经存在一定内容重合：一个说 tmpfs 数据可落盘，一个解释一般 swap 机制。二者不应在评价中算成两个独立的 `docker_swap` 新知识。
- 对照 V 的持久卷生命周期 `[24311,24414)` 不对应内存换出行为，但全文核对仍能确认它相对 V 是新增。

## covered：五条 additional_evidence

### S [0] `[1974,2100)`：容器超出内存限制可能出问题

- 已读 T `[629,991)` 已经更具体说明 tmpfs 计入限额、填满仍可能 OOM。**候选的内存风险概括已被历史覆盖**，不能把弱化概括当成新知识。
- 句中额外包含 CPU shares，但这是当前 tmpfs 内存/落盘目标以外的内容，不能凭这个新词证明有效补充。
- 结论：**没有目标相关新增**；该 possible_supplement 判断不成立。

### R [0] `[3840,4006)`：memory-reservation 的软限额与触发条件

- T 讲硬性的 memory/Compose mem_limit 与 tmpfs-size，没有 reservation 或低内存/争用触发规则；V 也没有。
- **未覆盖，对内存限制子问题有新的扩展价值**。不能仅因历史已有“memory limit”就判同义重复。
- 当前历史 T `[629,991)` 比 partial 的 volume 生命周期对照更接近，但还需读 R `[3840,4195)` 的完整表项，明确比较的是不同类型限额。

### R [1] `[4102,4195)`：软限额并非不可超过

- 与上一条一样，**软限额性质未被 V+T 覆盖，属于目标相关扩展**，必须保持 `memory-reservation` 的限定。
- 历史 T `[760,916)` 的“较大 tmpfs-size 不增加 RAM、仍可 OOM”谈硬约束与挂载容量，不能与该句混成互相否定。两条 additional 是同一软限额概念的两部分，不应虚增为两个完全独立的学习收益。

### K [0] `[1138,1306)`：某次 WSL2 Docker 设置 -m 后没有出现同样的问题

- V+T 没有这项特定环境观测，不能称其已经逐项覆盖。
- 但它是**提问者对普通文件复制页缓存的本机观察**，不是 tmpfs 的通用事实。正文结尾 `[6078,6309)` 又指出原案例内存本来就不足，改为更大限额解决。
- 结论：未读案例细节，但**没有给出当前目标的新 tmpfs 边界**。不能据此推出“设置 -m 就能避免 tmpfs OOM”。

### K [1] `[1307,1377)`：同一程序未设置 -m 时内存增长到数 GB

- V+T 未记录这个程序的无 -m 观测，因此不是历史中同一案例的重复。
- 它仍不是 tmpfs 数据的计账或 swap 规则；缺少 tmpfs 条件，且有本地环境限定。
- 结论：**没有目标相关新增**。它与 K [0] 是同一故障对照的两句，不宜当成两个有效学习点。

## uncertain 与 partial 状态的边界

- covered R `[4815,4959)` 的 swap 一般说明被放入 `uncertain_evidence`，没有宣称新增。已读 T `[1297,1462)` 已覆盖本目标所需的“临时数据可能经 swap 落盘”，所以这句话对核心目标不再新；R 全文的 `--memory-swap` 配置细节仍可能新，但并不包含在这条句子中。不能把 uncertain 虚计成错误新增。
- partial T `[530,627)` 的停止后不保留，是 V `[1995,2227)` 已知非持久性的具体生命周期说明；T `[1297,1362)` 的 Linux tmpfs 映射与 V `[20508,20775)` 的一般本地 mount 机制有相关基础，但 V 并未明确写出 tmpfs 映射。二者当前都未宣称新增，且都不是本目标缺失的两个核心边界。
- 三案 `semantic.status` 均为 `partial`，`error_code` 为 `semantic_units_unsupported`，字段 `unsupported_units` 分别是 **1、4、4**。只读代码可见此计数汇集文本转换、特征/推断调用等失败；它不提供逐条失败来源，**不能仅凭这个汇总数确认是哪些原文结论失去支持**，也不能将它等同于整个场景没有有效推荐。
- 本次正文审查仍可确认 partial 的 T 两个核心新增事实确实存在且此前未覆盖。同时，运行记录不足以证明所有候选/历史单元均完成模型比较，不能把本次原文复核包装成模型已完整支持每项关系。

## 结论

这组三案的主材料选择明显具备实际目标价值：cold 和 partial 都找到 T，partial 真正提取到了此前未读的内存计账与 swap 落盘说明。与此同时，列表混入一般卡顿/页缓存材料，部分匹配历史段落与候选命题不对应；covered 仍把已读风险的泛化概括标为补充。

covered 并非必然要零结果：R 的软/硬限额与具体 swap 配置有未读、目标相关的扩展内容；不能因 T 已读就将所有容器内存材料一概判重复。但实际 additional 只展示软限额性质，不能声称已经展示未选中的禁止 swap 配置。

以上是冻结保留集的独立事实审核。本轮不据此调整实现、模型或阈值，也不推断用户已经学会或获得实际学习收益。
