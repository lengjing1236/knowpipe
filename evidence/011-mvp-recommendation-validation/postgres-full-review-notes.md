# PostgreSQL 三场景独立原文审核

审核者：`/root/mvp_language`（代理原文审核，不是人类标注或学习效果实证）。日期：2026-09-25。

范围仅为 `current-v6-full.json` 的 `postgres_retry_cold`、`postgres_retry_partial`、`postgres_retry_covered`。没有运行模型、Spark，没有修改生产代码或主 review 文件。逐篇阅读五份涉及的完整正文，并将三个场景中 79 处正文引用与冻结语料的实际切片核对，全部一致。这里审查的是“引用内容能否支持结论”，并非仅检查偏移存在。

运行中的 JSON 仍在追加其他场景，本备注**不绑定也不声称有最终文件 SHA**。以下是本次已审核的独立 case 对象 SHA-256，计算方式为 `json.dumps(case, ensure_ascii=False, sort_keys=True, separators=(',', ':')).encode('utf-8')`，方便最终报告检查对应场景是否有变化：

| 已审核 case | SHA-256 |
|---|---|
| postgres_retry_cold | `d9c4b8c7af990469b12f667cd5796b866587771b30e7057185edba3e3b3b5f9a` |
| postgres_retry_partial | `0bf152bad6a824f1f3d18079dfc77cc2e9ee10977413abfaabf099d9557b7c14` |
| postgres_retry_covered | `0580ddfc4c6f537edfac1a9f054fd5318a5e763cd50c4e2839613af77e4e8459` |

当前 run 的 manifest SHA 为 `9faac4ed5d24d273f14c550d20edc30b9b7512c856e6db1b86b5be274b9d1b8f`；算法为 `goal-native-context-evidence-coverage-mmr-v6`，语义处理器 `semantic-c16d373f08c04fda5d6b947a167b6bd63ea93f22cc48c49077be89ef39a555de`，目标处理器 `llama-qwen-374fe0c53f97728d9a5ca437eca09f451565ed7a79c103e7d641b00608b0c9bd`。完整最终结果与正式 review 的绑定由评价代理完成。

同一规范化 JSON 规则下，当前 `signature` 对象 SHA-256 为 `f6269207efe9d68a50192810794d8d7f5a11c1589a6631a8d6d423d61c5699ac`，它是运行配置签名摘要，不是整个结果文件摘要。

共同目标：理解 PostgreSQL 序列化失败和唯一键冲突分别在什么情况下应该重试事务。判断“直接帮助目标”须提供失败分类、重试条件或边界；仅含 PostgreSQL、唯一约束、事务回滚不充分。

## 原文定位约定

语料：`state/feature009/corpus-expanded/documents.jsonl`。偏移均为 `body_text` 的 Unicode 码点、左闭右开 `[start,end)`。

| 简称 | 原始 doc_key（source:doc_id） | 结果中的 doc_key |
|---|---|---|
| H | `postgresql_docs:18/transaction-iso.html` | `b4fcd9990ef3800115020c7b193ae81e5f5c457e6888d37a83893e9f24393186` |
| R | `postgresql_docs:18/mvcc-serialization-failure-handling.html` | `013995f0037a6d31ae147406917508ad3c858ee98ea295b1c17a4f8fcae9a99a` |
| P | `stackexchange:dba-334904` | `ff7ba238922b567a73667bae41a1fb87c2f956e866511e4678ba9b65df57d83c` |
| D | `django_docs:topics/db/transactions` | `b436735ecf00e3bd088e2cb33cb5540afaf713af3eda4ee368b81f975c14a0e4` |
| U | `stackexchange:stackoverflow-79933630` | `7175306d2c9de3538594152120e4c21786494ff934f4b2f0e3102ae4bc9d8168` |

## 每项正文是否直接帮助目标

| 场景与排名 | 正文 | 判断 | 原文依据 |
|---|---|---|---|
| cold #1；partial #1；covered #1 | P：唯一约束违反的性能成本 | **相关但不直接回答目标** | `[92,1114)` 是业务已存在键时不插入的需求；`[3739,4731)` 比较 INSERT、ON CONFLICT、膨胀与回滚成本。推荐所引用 `[5373,5505)` 只谈先插入再回滚的实现，没有序列化失败与唯一键错误的重试条件。文中避免重复工作可作为后续性能背景，不能当本目标的主答案。 |
| cold #2；partial #3 | R：序列化失败处理 | **直接帮助目标** | `[40,451)` 说明 40001 与重试；`[563,1634)` 说明 23505/23P01 的并发情形以及持久错误边界；`[1636,1889)` 要求重试整个事务。完整正文确实提供所需区分。 |
| cold #3；partial #2；covered #2 | D：Django 事务 | **相关但不直接回答目标** | `[10629,10692)` 将该节限定为自定义事务管理；`[10694,10969)` 描述异常后当前事务中的 SQL 被拒绝；`[10988,11765)` 提供整个事务/保存点回滚。没有区分 40001、23505 及各自何时可重试。能够恢复事务状态，不等于已经解释重试政策。 |
| covered #3 | U：唯一约束与 NULL | **不直接回答目标** | `[58,280)` 是 Spring Boot/Liquibase 的可空唯一列需求；`[412,1077)` 解释 PostgreSQL 默认允许多个 NULL。与序列化失败、唯一键失败的重试条件无关。选中 `[502,665)` 的基础唯一性定义也没有补足目标。 |

按上述严格“直接帮助目标”的口径，cold 为 **1/3**，partial 为 **1/3**，covered 为 **0/3**。这并不否认 P、D 的一般技术价值，而是说明它们不应占据当前重试条件目标的主要推荐位置。

## 六条 additional_evidence 逐条审核

### partial #1：P `[5373,5505)`

- 内容：唯一约束通过先插入记录、违反约束时回滚实现。
- **给定 H 全文没有覆盖这一物理实现细节**。H `[20518,21639)` 确实已经覆盖并发插入造成唯一键冲突，但没有解释先写入行再回滚和死元组成本；不能仅因同谈唯一键就称整个细节已覆盖。
- 然而它**没有直接帮助判定何时应重试**。当前选作对照的 H `[3011,3367)` 实际讲序列计数的变化不会随事务回滚；其中 focus `[3011,3104)` 更只是“某些类型/函数有特殊事务行为”的总起句。对象和命题不对应。
- 结论：可能是未读的实现背景，但**不能据此成立当前目标的个人补充推荐**；这是目标相关性与比较依据失败，不宜误记为一个已读重复错误。

### partial #2：D `[10694,10853)`

- 内容：PostgreSQL 游标异常后，同一事务的后续 SQL 会报 aborted 错误。
- H `[12290,12430)` 已说明序列化错误后要中止并从头重试事务，覆盖“失败事务不能当作正常事务继续使用”的大方向；**H 未明确覆盖一般 IntegrityError 后所有后续 SQL 的特定报错行为**。因此不能称该句的所有细节都已读覆盖。
- 它仍**没有说明序列化失败/唯一键失败各自应重试的条件**。当前对照 H `[3011,3367)` 是序列计数的回滚例外，与候选的事务 aborted 状态不是同一命题；无从支持具体个性化差异说明。
- 结论：有局部新操作细节，但**不是当前目标的有效新增证据**。不能把“新细节”和“对目标有用的新知识”混为一谈。

### partial #3：R `[563,769)`

- 内容：某些情况下可重试唯一键/排斥约束失败，并给出 `23505` / `23P01`。
- H `[19820,20174)` 已有 `40001` 的序列化失败处理；H `[20811,21639)` 已有并发唯一键冲突及检查/生成新键的例子。**H 没有给出 `23505` / `23P01` 及排斥约束失败的可重试提示**，因此这一条不能整体判为已覆盖。
- 判为**部分新、直接相关**，可支持有限的候选补充。不过选中句只说“某些情况下”，尚未回答最重要的“哪些情况”。完整 R 正文 `[1404,1634)` 才明确提醒：序列化错误可直接重试，而其他错误可能是持久错误，不能同样对待。这段关键边界未进入当前 additional_evidence。
- 结论：六条中仅此条有明确的目标相关新增成分，但**证据解释不完整**。推荐完整 R 文档是有效的，不能据当前一句宣称已完整解释新旧差异或覆盖 `pg_retry_boundary` 的全部判据。

### covered #1：P `[5373,5505)`

- 已读集合增加 R 全文后，R 仍未描述“先插入物理记录再回滚”的实现。因此这里仍不是已被 H+R 完整覆盖的同义句。
- 但是当前目标需要的重试政策已在 H+R 中。这个物理实现细节仍不提供新的重试条件；对照还停留于 H 的序列回滚例外 `[3011,3367)`。
- 结论：**不支持有效的目标补充**，主要问题是相关性和比较对象，不能因为历史增加便随意标为全文重复。

### covered #2：D `[10694,10853)`

- H+R 已覆盖失败事务整体重试的原则，但没有明确描述一般 IntegrityError 后后续 SQL 的特定报错字符串；仍有局部操作细节不能直接认定覆盖。
- 对当前“何时重试”的目标没有新增条件，选中的 H `[3011,3367)` 也不是对应命题。
- 结论：**不支持有效的目标补充**。不能用接口层错误表现替代错误分类和重试条件。

### covered #3：U `[502,665)`

- 内容只是“多行在唯一约束列上的值相等会违反唯一约束”的概括。
- H `[20811,21029)` 已讲并发事务插入相同键导致唯一约束冲突，`[21183,21639)` 给出了先检查/选择新键再插入的例子；R `[770,1001)` 再次明确两个实例并发选择同一个新键会冲突。因此**对本学习目标而言，这条基础概括已由已读具体例子覆盖**，不能因换成一般定义就当作新重试知识。
- 严格区分：H+R 未逐字写出“所有约束列”的一般形式，所以本判断不是形式逻辑上证明 U 的所有定义细节均被逐字蕴含；它是对同一基础概念的概括不构成有效新增的判断。U 的 NULL 规则可能确实未读，但不在这条 additional_evidence 中，也不回答目标。
- 结论：**基础概念已覆盖，且没有新增目标价值**。不应将这条展示为个人补充。

## 额外核对：不要把已读例子包装成新增

partial 的 R `[770,1001)` 是并发选中同一个新主键值的例子；H `[20518,21639)` 已解释相同机制，包括“先检查键不存在”“读取最大键后加一”的具体情形。这部分**已覆盖**。

本轮算法将它放在 `uncertain_evidence`，并没有将它列入 `additional_evidence`，因此不能虚构成一次“错误新增”来扣分；但其实际不确定性可以经全文审核消除，也不能作为新内容贡献。历史标题或命中句不足以判断此事，必须阅读整段例子。

## 独立审核结论

- 冷启动没有虚称个人新增，正确；核心相关资料 R 进入 top 3，正确。但有两项只与主题词接近，冷启动列表的直接目标命中率仍低。
- partial 中确有一项有效完整阅读材料 R，但六条 additional_evidence 总共只有一条包含明确的目标相关新增成分；还没有展示最关键的持久错误边界。其余条目的新颖性不能弥补目标不相关。
- covered 排除了已读 R 文档，符合排除规则；剩余三项却仍全部宣称 possible_supplement。逐项审核不支持这些条目作为当前目标的有效新增，其中 U 是已读具体例子的基础概括。应允许“本次没有找到可靠的新内容”，而不是为了填满列表给出弱相关补充。
- 以上为代理对冻结原文的语义审核，未观测用户学习效果，也未运行数据库来验证原文陈述本身的技术正确性。
