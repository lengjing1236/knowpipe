# 课程项目选题调研、实施方案与计划

## 0. 一句话结论

建议采用 **Stack Exchange 多站点技术问答作为规模主源，arXiv Computer Science 摘要元数据作为异构补充源，Spark MLlib 完成可解释的 TF-IDF/关键词与个性化排序，MongoDB 保存原文和结果，后端提供只读推荐 API，Web 页面展示推荐理由与知识差分**。

这条路线以可运行和可答辩为优先，再用“跨来源知识桥接、知识缺口解释、可复现实验凭证”形成稳中有亮点的效果。

## 1. 可公开汇报的选题调研结论

### 1.1 项目要解决的问题

技术内容分散在问答社区和学术预印本中。用户通常知道自己已经掌握的主题（例如 Python、Spark、MongoDB），但很难发现：

- 与已有知识相邻、但自己尚未接触的主题；
- 同一技术在工程问答和学术论文中的不同表达；
- 内容质量、来源和推荐理由是否可信。

项目目标不是生成一段摘要，而是建立一条可验证的数据链路：

```text
公开技术文本 → Spark 批处理/挖掘 → MongoDB 文档管理 → 推荐 API → Web 交互与解释
```

### 1.2 选题价值

- **课程价值**：覆盖采集、批处理、数据管理、挖掘、后端和 Web 展示。
- **技术价值**：使用 Spark 计算文本特征和排序分数，而不是把 LLM 输出当作算法结果。
- **应用价值**：帮助学习者从已掌握主题向邻近主题扩展。
- **答辩价值**：每个推荐结果都能解释“来自哪里、匹配了哪些词、为什么排在前面”。

### 1.3 数据调研结论

| 数据源 | 已核验情况 | 在项目中的角色 |
|---|---|---|
| Stack Exchange API | API 可访问；Stack Overflow、Server Fault、Super User、Ask Ubuntu 的问题数均远超 1 万；记录含正文、标签和 `content_license` | 主数据源，满足规模和工程技术文本要求 |
| Stack Exchange Data Dump | 官方入口存在，但当前环境未核实最新压缩包清单和下载可用性 | 若教师允许且环境可用，替换 API 作为批量输入 |
| arXiv API | `cs.*` 查询在 2026-09-03 返回 931,692 条；Atom 记录含标题、摘要、作者、分类；官方说明 metadata 为 CC0 | 补充数据源，体现学术来源异构性 |
| CodeSearchNet | 官方 README 说明约 200 万条 comment-code 对、JSONL、六种语言；源代码许可证按仓库变化 | 特色备选，不作为第一阶段必需依赖 |
| OpenAlex / Crossref | 可提供学术元数据，但许可和摘要完整性需要逐条处理 | 后续补充或对照，不计入核心实现 |
| Kaggle Python related StackOverflow posts | 页面显示 CC BY-SA 4.0、文件约 155 MB，但记录数未核验 | 备用数据源，不在未核验前计入规模 |
| GH Archive、B 站、RSS | 分别是事件归档或公开内容来源，缺少稳定文档规模和统一许可 | 不作为主源；避免把公开可见误写为可自由使用 |

### 1.4 合规底线

1. 保存来源 URL、采集日期、原始许可证和数据版本。
2. Stack Exchange 记录按自身 `content_license` 处理，不能统一假设所有帖子同一许可证。
3. arXiv 只把 metadata 按 CC0 处理；论文全文和 PDF 仍按单篇许可判断。
4. 课程展示优先展示标题、短文本片段、关键词和来源链接；不默认公开再分发完整全文。
5. 在规模证明中区分“API 查询总数”“实际采集行数”和“去重后可用文档数”。

## 2. 推荐的最小可行方案（MVP）

### 2.1 数据范围

第一版只保留两个来源：

- **Stack Exchange**：四个站点的带正文问题，统一为问答文档；
- **arXiv**：`cs.*` 中带摘要的论文元数据，统一为论文摘要文档。

建议的阶段性规模：

| 阶段 | Stack Exchange | arXiv | 目标 |
|---|---:|---:|---|
| 技术切片 | 100 条 | 100 条 | 验证字段、清洗、Spark、MongoDB、API 和页面闭环 |
| 小规模演示 | 2,000 条左右 | 500 条左右 | 验证排序、去重和展示效果 |
| 课程规模 | ≥10,000 条可用文档 | 作为补充扩展 | 留出删除、去重和缺失后的余量 |

这里的“课程规模”必须以实际采集并成功写入 MongoDB 的行数为准，不以平台宣传数量代替。

### 2.2 统一文档结构

不要求两个来源原始字段完全相同，而是在入库前建立最小统一结构：

```text
doc_id              唯一标识
source              stackexchange 或 arxiv
source_site         站点或 arXiv 分类
title               标题
body_text           清洗后的正文或摘要
body_raw            原始 HTML/XML 文本（按合规需要保留）
tags                标签或分类
language            语言
created_at          创建/发表时间
source_url          原始链接
license             记录级许可证或 unknown
quality             长度、重复、缺失等质量信息
mining              Spark 生成的关键词、主题、向量摘要和分数
```

MongoDB 中保留原始字段和统一字段的理由是：既能进行跨源查询，也能在答辩时追溯到原始记录。

### 2.3 核心挖掘算法

第一版只实现一种主算法，保证可解释：

1. Spark 读取两个来源的 JSON/XML 转换结果。
2. 清洗 HTML、代码块、标点和停用词，保留技术词和标签。
3. 使用 Spark MLlib 的 TF-IDF 或 `CountVectorizer + IDF` 生成文档特征。
4. 为每篇文档提取 top-k 关键词。
5. 用户显式选择已有主题，例如 `Python`、`Spark`、`MongoDB`。
6. 计算推荐分数：

   ```text
   recommendation_score
     = α × topic_match
     + β × text_similarity
     + γ × quality_score
     + δ × novelty_score
   ```

7. `novelty_score` 只表示“与用户已知主题不同但仍相邻”，不代表事实新颖性。
8. 输出推荐文档、匹配关键词、来源、主题差异和分数构成。

不把 LLM 摘要作为核心算法。若后续使用 LLM，只能作为可选的展示解释，并明确与 Spark 计算结果分开。

### 2.4 个性化定义

为降低冷启动风险，第一版使用**显式主题偏好**：

- 用户勾选或输入已掌握标签；
- 标签映射为主题偏好向量；
- 推荐“相似且未读”以及“相邻但不同”的文档；
- 记录用户已读文档，避免重复推荐。

这一定义比依赖真实收藏历史更容易在短周期内实现和解释。后续如有时间，再加入阅读历史作为加分项。

### 2.5 MongoDB 和 Web 的最小范围

MongoDB 至少保存三个集合：

- `documents`：清洗后的原文、来源和许可证；
- `mining_results`：关键词、主题、特征摘要和批处理版本；
- `user_profiles`：用户选择的已知主题和已读文档 ID。

后端只需要只读或轻量写入接口：

- `GET /api/recommendations?topics=python,spark`：返回推荐列表；
- `GET /api/documents/{id}`：返回文档详情、来源和推荐理由；
- `GET /api/topics`：返回主题和关键词统计；
- `POST /api/profile/topics`：保存用户显式主题偏好。

Web 页面保持单页：主题选择、推荐列表、来源筛选、关键词权重、知识差分说明和数据规模统计。

## 3. “稳中求优秀”的亮点设计

亮点必须建立在核心链路完成后，且不改变主算法。

### 3.1 跨来源知识桥

当 Stack Exchange 问答和 arXiv 摘要共享高权重技术词时，显示“工程实践 ↔ 学术研究”的关联。例如：

```text
Stack Overflow: Spark DataFrame 性能问题
        ↓ 共享主题词：Spark、partition、query
arXiv: 分布式数据处理优化论文
```

这能直接体现“多源”价值，不需要复杂知识图谱。

### 3.2 推荐理由和知识差分

每条推荐展示：

- 已知主题：Python、Spark；
- 新增主题：Kafka、partition、streaming；
- 匹配关键词及权重；
- 来源站点和许可证；
- 与已读文档的相似度。

答辩时可以现场追问一条结果，系统能够回到具体字段和计算分数。

### 3.3 可复现实验凭证

在页面或报告中展示：

- 数据版本/采集日期；
- 输入条数、去重后条数、有效正文条数；
- Spark 运行模式、耗时和输出位置；
- MongoDB 集合及索引；
- 推荐评价指标和样本标注方法。

这是低开发量但高答辩价值的功能，能避免“只展示一个推荐页面”的印象。

## 4. 实施阶段与计划

以下按短周期项目安排，可根据实际周数压缩或拉伸。每一阶段都有可验收产物，避免最后才发现环境不可用。

### 阶段 0：条件闸门（半天至 1 天）

**必须确认：**

- Spark 版本、`spark-submit`、master 和运行模式；
- MongoDB 地址、账号权限和 Spark Connector；
- 教师是否认可 Stack Exchange API 与 arXiv metadata；
- “1 万条文档”是否接受摘要文档；
- 用户角色和个性化评价方式。

**闸门结果：**

- 若 Spark/MongoDB 均可用：进入正式切片；
- 若只有本地环境：先做 Spark local + MongoDB 远程连接验证；
- 若 MongoDB 不可用：暂停编码，不能声称完成课程链路；
- 若许可证或数据源不获批准：切换到教师认可的数据源，不继续扩大实现。

### 阶段 1：数据契约和样本（1 天）

产物：

- 两个来源各 100 条样本；
- 字段字典和许可证记录表；
- 统一文档结构；
- 去重、缺失、HTML 和语言质量检查结果。

验收：每条样本可追溯到原始 URL，正文或摘要非空，许可证状态明确或标记 `unknown`。

### 阶段 2：Spark 文本切片（1—2 天）

产物：

- Spark 输入/输出条数日志；
- 清洗前后样本对照；
- TF-IDF 特征和每篇 top-k 关键词；
- 主题统计表；
- 可重复执行的命令和运行参数记录。

验收：同一输入重复运行得到稳定的关键词和排序；能够解释一篇文档的关键词来源。

### 阶段 3：MongoDB 入库和查询（1 天）

产物：

- `documents`、`mining_results`、`user_profiles` 集合；
- 唯一键、来源和主题索引；
- 三条可复现查询；
- 实际入库数量统计。

验收：可以按来源、主题和文档 ID 查询，并能回到原始 URL。

### 阶段 4：推荐 API 和单页 Web（1—2 天）

产物：

- 主题偏好输入；
- top-k 推荐 API；
- 文档详情 API；
- 推荐列表、关键词权重、来源和知识差分页面。

验收：浏览器从主题选择到推荐结果全程可演示；API JSON 与页面显示一致。

### 阶段 5：评价和答辩材料（1—2 天）

产物：

- 人工相关性标注表或留出集；
- Precision@K、Recall@K、NDCG 或 HitRate@K；
- 与无个性化基线的对比；
- 端到端截图和运行日志；
- 需求规约、设计规约、AI 提示词记录和 Git 提交说明。

验收：能回答“为什么推荐它”“Spark 做了什么”“MongoDB 保存了什么”“多源体现在哪里”“算法如何评价”。

### 建议的 Git 提交节奏

```text
commit 1  数据源与许可证核验记录
commit 2  统一文档结构和样本证据
commit 3  Spark 清洗与 TF-IDF 结果
commit 4  MongoDB 入库与查询证据
commit 5  推荐 API 和页面
commit 6  评价、截图和答辩材料
```

每次提交只完成一个可说明的里程碑，避免最后一次提交包含全部功能。

## 5. 第一技术切片与验收标准

### 切片目标

用 100 条 Stack Exchange 问题和 100 条 arXiv 摘要，完成：

```text
采集样本 → 统一字段 → Spark 清洗和 TF-IDF → top-k 关键词
→ 写入 MongoDB → API 返回一个用户的推荐列表
```

### 必须提供的证据

1. 两个来源的 URL、采集日期、许可证字段和原始条数；
2. 统一字段样例各 3 条；
3. Spark 命令、运行模式、输入输出条数和耗时；
4. 一篇文档的清洗前后文本和关键词权重；
5. MongoDB 集合、索引和查询输出；
6. API JSON；
7. 页面截图；
8. Git 提交号和对应规约记录。

### 失败判定

- 只能展示 API 原文、没有 Spark 输出：切片失败；
- 只有 MongoDB 增删改查、没有可解释特征或排序：挖掘要求未满足；
- 只有摘要生成、没有 Spark 算法：不能作为核心成果；
- 数据规模只引用平台宣传数量、没有实际入库统计：规模证据不足；
- 许可证为未知却公开展示全文：合规闸门失败。

## 6. 风险与最小应对

| 风险 | 影响 | 最小应对 |
|---|---|---|
| Spark 环境不存在 | 无法满足课程硬约束 | 阶段 0 先验证集群；未验证前不承诺可运行 |
| MongoDB 或 Connector 不可用 | 无法完成数据管理链路 | 先取得实例和权限；必要时由课程环境提供 |
| API 限流 | 采集时间变长 | 先固定样本和分页计划；Dump 可用时再切换 |
| Stack Exchange 许可证处理错误 | 合规和答辩风险 | 保存每条 `content_license`、URL 和署名信息 |
| arXiv 只有摘要 | 文档内容较短 | 明确标注“摘要文档”，不宣称论文全文 |
| 数据重复、HTML 和代码噪声 | 推荐质量下降 | 记录去重规则、正文长度阈值和清洗前后统计 |
| 没有真实用户行为 | 个性化评价困难 | 使用显式主题偏好和人工相关性标注 |
| 页面先做、算法后补 | 容易变成展示型系统 | 先完成 Spark 输出和评价，再做页面 |

## 7. 公开汇报建议

### 7.1 5 分钟汇报结构

1. **问题**：技术内容分散，已有知识与新知识之间缺乏可解释发现路径。
2. **数据**：Stack Exchange 问答 + arXiv CS 摘要；展示来源、规模和许可证。
3. **方法**：Spark 清洗、TF-IDF/关键词、主题匹配、相邻主题排序。
4. **存储与服务**：MongoDB 文档和挖掘结果；API 返回 top-k 推荐。
5. **演示**：选择 `Python + Spark`，展示一个 Stack Overflow 问题和一篇 arXiv 摘要的跨源推荐。
6. **评价**：与热门度或纯相似度基线比较 Precision@K/NDCG。
7. **计划与风险**：先完成技术切片，再扩展规模和亮点。

### 7.2 推荐的现场演示路径

```text
选择已有主题 Python、Spark
    ↓
查看推荐列表（来源、分数、关键词）
    ↓
筛选“工程问答”或“学术摘要”
    ↓
打开详情，查看已知主题/新增主题差分
    ↓
展开数据来源、许可证和 Spark 计算证据
```

### 7.3 可能的答辩问题

- 为什么这算 Spark，而不是普通 Python？
  - 因为清洗、特征构造和批量关键词/排序在 Spark 作业中执行，并提供运行日志。
- 为什么这算数据挖掘，而不是增删改查？
  - 因为 TF-IDF、关键词和相似度排序从文本中发现结构，并用指标评价。
- 个性化依据是什么？
  - 用户显式选择已知主题，系统推荐相似且未读、或相邻但不同的主题。
- 多源在哪里？
  - Stack Exchange 是工程问答，arXiv 是学术摘要，字段和文本风格不同，统一后仍保留来源。
- 为什么不把 LLM 摘要当算法？
  - 核心分数来自 Spark 可复现计算；LLM 若使用，只作为可选解释。
- 1 万条如何证明？
  - 提交实际采集数、去重后数、有效正文数、入库数和采集时间，而不是引用平台总量。

## 8. 当前不应作出的承诺

在 Spark 集群、MongoDB 实例、数据许可和课程数据规模口径确认前，不应对外宣称：

- 当前仓库已经实现 Spark 处理；
- 当前仓库已经连接 MongoDB；
- 已经有 1 万条可展示的课程数据；
- B 站或 RSS 内容可以自由抓取和再发布；
- 推荐结果已经通过真实用户实验验证。

这些都是实施完成后才可用运行证据支持的结论。

## 9. 开始编码前的五项确认

1. 教师是否批准“Stack Exchange API/转储 + arXiv metadata”，以及摘要是否算文档？
2. Spark 的版本、`spark-submit`、master 地址、运行模式和资源配额是什么？
3. MongoDB 实例、账号权限、版本及 Spark Connector 是否可用？
4. 个性化是否采用显式主题偏好，用户角色和评价指标由谁确认？
5. 需求规约、设计规约、AI 提示词记录、Git 远程仓库和最终部署环境有什么模板或限制？

## 10. 最终建议

先把项目目标限定为一个可验证的“跨来源技术知识发现”闭环：

**两类真实技术文本 + 一个 Spark 可解释算法 + 一个 MongoDB 文档库 + 一个只读推荐页面。**

核心闭环稳定后，再加入跨来源知识桥、知识差分解释和实验凭证。这些亮点不会改变数据链路和主算法，能够在短周期内提升展示效果，同时保持实现和答辩风险可控。
