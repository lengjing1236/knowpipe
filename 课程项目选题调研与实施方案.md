# 课程项目选题调研、实施方案与三周计划

> 项目名称：基于 Spark 与 MongoDB 的多源技术内容知识挖掘与个性化发现 Web 系统  
> 团队成员：陶文杰、陈铭宇  
> 调研日期：2026-09-03  
> 文档状态：选题确认与人工评审稿，尚未进入编码实施

## 文档说明

本报告依据以下材料整理：

- [README.md](README.md)
- [DESIGN.md](DESIGN.md)
- [audit_report.md](audit_report.md)
- [feasibility_report.md](feasibility_report.md)
- [public_dataset_research_report.md](public_dataset_research_report.md)
- [大数据综合实践课程内容说明.pdf](大数据综合实践课程内容说明.pdf)
- [大数据综合实践课程参考资料.pdf](大数据综合实践课程参考资料.pdf)

文中采用以下状态标记，避免把规划写成已经完成的事实：

- **[课程要求]**：来自课程 PDF 的约束或评分原则；
- **[已验证]**：仓库材料、本轮环境检查或公开数据调研已有证据；
- **[方案]**：建议采用、尚待实现的项目设计；
- **[待确认]**：必须由教师或项目成员进一步确认。

当前本机已验证具备 Java 17、Python 3.10 和 Flask，但没有 `spark-submit`、PySpark、MongoDB、PyMongo或容器运行时。现有 Knowpipe 是可运行的本地知识精炼 CLI，尚不是完成了 Spark、MongoDB 和 Web 的课程系统。

---

## 1. 公开汇报用的选题结论

### 1.1 项目要解决的问题

技术知识分散在 Stack Exchange 工程问答、arXiv 学术论文等来源中。学习者虽然已经掌握了一部分主题，但普通搜索和热门推荐通常无法回答：

1. 哪些内容是用户已经掌握、无需重复阅读的？
2. 哪些内容能深化已有知识？
3. 哪些相邻主题构成真正的新知识？
4. 推荐结果为什么出现，其数据来源和计算依据是否可信？

项目目标是构建一条可运行、可解释、可评价和证据可追溯的知识发现链路：

```text
多源技术文档 → Spark 数据处理与挖掘 → MongoDB 文档管理
→ Web API → Web 交互与可视化 → 个性化知识发现
```

### 1.2 目标用户

- 需要持续学习技术知识的计算机专业学生；
- 希望追踪相关工程问题和学术进展的开发者；
- 需要根据已有知识过滤重复内容的自主学习者。

### 1.3 推荐选题方案

**[方案]** 采用 Stack Exchange 多站点技术问答作为规模主源，采用 arXiv Computer Science 标题与摘要元数据作为稳定异构补充源。Spark 负责批量文本清洗、TF-IDF、关键词和相似度计算；MongoDB 保存原始文档、处理结果、用户知识画像和反馈；Flask 提供业务 API；Web 页面展示主题选择、推荐列表、来源、关键词以及新知识/深化知识解释。

### 1.4 个性化机制

**分类的对象是从文档中抽取出的知识单元（关键词/术语），不是整篇文档**：同一篇文档可以同时包含用户已知和未知的内容，判定必须落在知识单元这一粒度，而不是给整篇文档贴一个标签。

判定分两层：

- **基础匹配（关键词级）**：Spark 对每篇文档做 TF-IDF，取高权重关键词集合，与用户已知关键词集合比较重合度和新增比例。这是精确字符串匹配，速度快、可解释，但换了个说法的同义词（如 "sharding" 与 "partition"）会被误判为全新知识（假阴性）。
- **聚类补充（主题级）**：对文档的 TF-IDF 向量做 KMeans 聚类得到主题簇。用户已读文档所属的簇记为"已知主题"；新文档即使关键词不完全重合，只要落在已知簇内，也按已知/深化处理，缓解关键词级匹配的假阴性。

用户的显式已知主题、已读文档、已确认知识卡和反馈共同构成个人知识画像（关键词集合 + 主题簇集合）。综合两层信号，对每个知识单元形成以下结果：

- `known`：关键词高度重合，或命中已知主题簇且无明显新增，默认过滤；
- `refine`：命中已知主题簇（或关键词部分重合），同时出现明显新增内容；
- `new`：关键词基本不重合，且未命中任何已知主题簇；
- `possible_conflict`：只在有可追溯断言和判定证据时展示，并要求人工复核。

### 1.5 课程价值

- Spark 真实参与清洗、特征提取和批量计算，而非作为技术名词装饰；
- MongoDB 管理问答、摘要、挖掘结果和用户知识记录等文档数据；
- Web API 和页面构成完整业务与交互层；
- 核心算法可解释、可复现并可用指标评价；
- 数据条数、Spark 日志、MongoDB 查询、API 输出、页面截图和 Git 提交均可形成答辩证据。

---

## 2. 最终数据源建议

### 2.1 主数据源：Stack Exchange 技术问答

#### 已验证的规模

公开数据调研在 2026-09-03 通过 Stack Exchange API 得到以下动态统计：

| 站点 | 问题数 | 答案数 |
|---|---:|---:|
| Stack Overflow | 24,249,994 | 36,102,348 |
| Server Fault | 329,147 | 522,660 |
| Super User | 516,635 | 753,329 |
| Ask Ubuntu | 425,414 | 540,329 |

这些平台统计只能证明来源容量充足，不能替代项目实际采集和入库数量。项目应把一条带正文的问题定义为一条文档，不能把问题数与答案数直接相加制造规模。

#### 获取方式

1. 教师批准且下载条件允许时，优先使用官方 Data Dump；
2. Data Dump 不可用时，使用 Stack Exchange API 分页采集；
3. API 配额不足时，申请 API Key、分日采集，或切换到教师批准的数据集；
4. 不使用记录数和原始许可证尚未核验的第三方整理数据代替最终主源。

#### 建议字段

```text
source_site, post_id, title, body_html, body_text,
tags, score, view_count, answer_count, accepted_answer_id,
created_at, source_url, content_license, collected_at
```

#### 是否计入一万条

**计入，但必须以实际数据为准。**

**[方案]** 采集约 12,000～15,000 条原始问题，为重复、正文缺失、低质量文本和解析失败留出余量。最终验收口径为：

> 清洗后有效、唯一且成功写入 MongoDB 的 Stack Exchange 文档不少于 10,000 条。

#### 许可证边界

- API 记录包含 `content_license`，历史帖子可能采用不同版本的 CC BY-SA；
- 必须逐条保存许可证、原始 URL 和必要的署名信息；
- 不能统一假设所有历史帖子均采用 CC BY-SA 4.0；
- 页面优先展示标题、必要片段、关键词和原文链接；是否展示完整正文需教师确认。

#### 失败替代方案

- 第 4 天前仍不能获得稳定样本：切换到教师统一提供且许可证明确的文档数据；
- API 不稳定但官方转储可用：改用转储并记录版本；
- 转储过大：优先使用较小技术站点或受控 API 样本，不盲目下载全量 Stack Overflow 数据。

### 2.2 稳定补充源：arXiv Computer Science 元数据

#### 已验证的规模

2026-09-03 的 arXiv API 查询 `cat:cs.*` 返回约 931,692 条记录。该数字是动态查询结果，不代表项目已经下载或处理了这些记录。

#### 建议字段

```text
arxiv_id, title, summary, authors, categories,
primary_category, published_at, updated_at,
abstract_url, pdf_url, metadata_license, paper_license
```

#### 获取与用途

- 通过 arXiv Atom API 获取标题、摘要、作者、分类和日期；
- 用于形成“工程问答 + 学术研究”的异构数据；
- 用于从 Stack Exchange 工程主题扩展到相关论文；
- 基线不下载或公开展示论文全文。

#### 是否计入一万条

默认不依赖 arXiv 满足规模门槛，Stack Exchange 主源应独立达到 10,000 条。只有教师明确认可“标题 + 摘要元数据”为文档时，才把有效 arXiv 摘要计入补充规模。

#### 许可证边界

- **[已验证]** arXiv metadata 按当前官方说明采用 CC0；
- 论文 PDF 和全文许可按单篇作品区分，不能统一视为 CC0；
- 项目必须明确“摘要元数据”与“论文全文”的区别。

#### 失败替代方案

- 教师不认可摘要文档：arXiv 只作展示性补充，不计入规模；
- API 限流：缩小补充样本，主链路仍由 Stack Exchange 支撑；
- 需要更换补充源时，仅采用教师批准且许可清晰的数据集。

### 2.3 B 站与 RSS 的范围

| 来源 | 核心范围 | 是否计入 1 万条 | 进入条件 |
|---|---|---|---|
| B 站字幕/转写 | 否，惊喜功能候选 | 否 | 作者或平台使用边界明确，获取稳定，字幕质量可验证 |
| RSS 技术博客 | 否，提升或惊喜候选 | 否 | 逐站确认版权、正文可用性、历史数量和抓取条件 |

现有 Knowpipe 已有 B 站和 Podcast/RSS 输入适配器，这只能证明程序接口存在，不能证明内容可以批量保存、再发布或承担课程规模要求。

---

## 3. 课程要求映射

| 课程要求 | 项目功能 | 技术组件 | 可提交证据 | 当前状态 |
|---|---|---|---|---|
| 文档数据不少于 1 万条 | Stack Exchange 技术文档库 | 采集、质量统计、MongoDB | 原始数、去重数、有效数、入库数、采集日期 | 未完成；本地没有万条数据 |
| 大数据底层处理 | 批量清洗、特征构造和统计 | Spark Core、Spark SQL | `spark-submit`、master、executor、输入输出数、耗时 | 未完成；本机无 Spark |
| 数据挖掘 | TF-IDF、关键词、相似度和推荐排序 | Spark MLlib | 参数、关键词样例、指标、基线对比 | 有本地 TF-IDF 原型，无 Spark 规模实验 |
| MongoDB 文档管理 | 原文、结果和用户知识记录 | MongoDB、索引、Connector 可选 | 集合结构、索引、查询、入库统计 | 未完成；本机无 MongoDB |
| 个性化发现 | 已知过滤、新知识和深化知识 | 用户画像、向量、规则评分 | 画像样例、分数构成、top-k、评价 | 有产品概念，未形成 Web 推荐 |
| 后端业务层 | 文档、主题、画像和推荐 API | Flask | 启动命令、接口测试、JSON 输出 | 未完成；仅确认 Flask 可用 |
| Web 交互层 | 主题选择、推荐列表、关键词展示 | HTML/CSS/JavaScript、ECharts 可选 | 截图、录屏、API 与页面一致性 | 未完成 |
| 完整四层链路 | 数据→Spark→MongoDB→API→页面 | Spark、MongoDB、Flask、前端 | 端到端演示、日志、架构图 | 未完成 |
| SDD | 需求、设计、任务和变更记录 | GitHub Spec Kit | 需求规约、设计规约、任务清单 | 尚未发现 Spec Kit 项目结构 |
| AI 提示词 | 完整保留开发提示词 | Markdown/JSONL | 时间、用途、模型、提示词、结果摘要 | 现有代码有提示词，但未形成课程记录 |
| Git 历史 | 按里程碑提交 | Git、远程仓库 | 提交记录、Tag、远程地址 | 本地有 5 次提交，无远程地址 |
| 报告与演示 | 报告、PPT、备份视频 | Markdown、PPTX、录屏 | 文件、截图和演示脚本 | 本报告与 PPT 源开始补齐 |
| 两人真实贡献 | 陶文杰、陈铭宇分工与交叉答辩 | 分工表、Review 记录 | 初版/最终版差异、提交和答辩记录 | 待执行 |

**[课程要求]** 真实分布式集群在“大数据底层处理架构”评分上优于单机。课程材料没有说明 local 模式的具体扣分数，因此报告不虚构扣分比例。

---

## 4. 分布式与本机运行方案

### 4.1 真实分布式 Spark 主方案

#### 使用条件

- 教师提供或批准 Spark standalone、YARN 等真实集群；
- 已取得 master 地址、账号、网络、存储路径和 executor 配额；
- Spark、Python 与 MongoDB Connector 或中间文件格式兼容。

#### 提交方式示意

```text
spark-submit \
  --master <教师确认的 master> \
  --deploy-mode <教师确认的模式> \
  --num-executors <资源配额> \
  --executor-cores <资源配额> \
  <批处理入口>
```

以上参数当前都是占位符，不能写成已经获得的环境。

#### 运行证据

- Spark 版本、完整命令和应用 ID；
- master、deploy mode 和 worker/executor 信息；
- 至少两个实际计算节点或 executor 的任务分布证据；
- Spark UI 或 event log；
- 输入、输出、失败、分区和耗时统计；
- 清洗、TF-IDF、相似度计算和结果写回的 DAG；
- 对应 Git 提交与设计规约。

#### 收益与风险

真实集群能直接响应课程对分布式处理架构的评分要求，并展示任务划分、shuffle、缓存和 executor 扩展。当前风险是尚无集群地址、权限、存储路径或资源配额。

### 4.2 Spark local 降级方案

#### 触发条件

- 最迟第 2 天仍无法获得真实集群；
- 集群持续不可访问或不能连接项目数据；
- 教师明确同意 local 模式作为备用路径。

#### 运行方式

```text
spark-submit --master local[*] <批处理入口>
```

#### 要求与局限

- 仍须用真实 Spark DataFrame、SQL 和 MLlib 完成清洗与特征计算；
- 与集群方案使用相同数据契约和输出格式；
- 保存 Spark UI、日志、条数和耗时；
- 报告、PPT 和答辩中明确标注“Spark local 单机模式”；
- local 多线程不等于多节点分布式，不能声称验证了节点容错或网络 shuffle。

### 4.3 本机 MongoDB 与 Web

当前本机没有 MongoDB 二进制、PyMongo 或容器运行时。后续获得安装或实例权限后：

- MongoDB 保存 `documents`、`mining_results`、`user_profiles` 和 `user_knowledge`；
- `source + doc_id` 建唯一索引，主题、标签、来源和时间建立查询索引；
- Flask 后端连接 MongoDB，提供文档、主题、画像和推荐 API；
- Web 页面在本机浏览器运行，版本、端口和启动方式写入 README。

Spark 写入 MongoDB的顺序：

1. 首选经过版本验证的 MongoDB Spark Connector；
2. Connector 不兼容时，Spark 输出带批次 ID 的 JSON/Parquet，再由幂等导入程序写入 MongoDB；
3. 无论采用哪种方式，都必须证明 MongoDB 中保存的是实际 Spark 输出。

如果无法取得 MongoDB 实例或安装权限，不能使用 SQLite 替代后声称满足课程要求，应立即向教师申请环境。

### 4.4 推荐路径

- **主路径**：真实 Spark 集群 + 本机或课程 MongoDB + 本机 Flask/Web；
- **备用路径**：经教师同意的 Spark `local[*]` + 本机 MongoDB + 本机 Flask/Web；
- **当前事实**：两条路径均未取得完整运行证据。

---

## 5. 最小实施方案

### 5.1 最小完整链路

```text
Stack Exchange 问题 + arXiv 摘要
        ↓
统一数据契约、许可证字段和质量标记
        ↓
Spark：清洗、去重、TF-IDF、top-k 关键词、相似度
        ↓
MongoDB：原文、挖掘结果、用户知识记录
        ↓
Flask API：画像、文档、主题、推荐
        ↓
Web：选择已有知识 → 展示新知识/深化知识及推荐理由
```

第一技术切片使用两个来源各 100 条文档。只有字段、Spark、MongoDB、API 和页面全部贯通后，才扩展到不少于 10,000 条。

### 5.2 统一文档结构

```text
doc_id              唯一标识
source              stackexchange / arxiv
source_site         站点或 arXiv 分类
title               标题
body_text           清洗后的正文或摘要
body_raw            原始 HTML/XML（按合规要求保留）
tags                标签或分类
language            语言
created_at          创建或发表时间
source_url          原始链接
license             记录级许可证或 unknown
quality              长度、缺失、重复和解析质量
batch_id             Spark 批次与算法版本
mining               关键词、主题、特征摘要和分数
```

### 5.3 核心挖掘与个性化

知识抽取分两层，均由 Spark 批处理产出，写入 `mining_results`：

- **关键词层**：TF-IDF 取每篇文档的 top-k 关键词及权重；
- **主题层**：对文档 TF-IDF 向量做 KMeans，得到 `topic_cluster_id`。

个性化判定（known/refine/new）在文档的关键词集合与用户已知关键词集合、以及文档 `topic_cluster_id` 与用户已知主题簇集合两个信号上综合给出，取代单一“文档级”标签。

在此基础上，推荐排序分数建议采用：

```text
score =
  α × 与已有知识的主题相关度
+ β × 新增关键词比例
+ γ × 文档质量
+ δ × 来源多样性
- ε × 已读或高度重复惩罚
```

权重是待验证的实验参数，不在实施前宣称最优。第一版至少设置“热门度/纯相似度”作为非个性化基线，并使用 Precision@K、NDCG 或人工相关性评分比较。

### 5.4 MongoDB 最小集合

| 集合 | 主要内容 | 核心索引 |
|---|---|---|
| `documents` | 原文、统一字段、来源和许可证 | `source + doc_id` 唯一索引；来源、标签、时间索引 |
| `mining_results` | 关键词、向量摘要、分数和批次版本 | `doc_id + batch_id`；主题、分数索引 |
| `user_profiles` | 用户显式主题、已读文档和反馈 | `user_id` 唯一索引 |
| `user_knowledge` | 知识卡（`knowledge_id` 为关键词或主题簇 id）、掌握状态和来源 | `user_id + knowledge_id`；主题索引 |

### 5.5 最小 API 和页面

建议 API：

```text
GET  /api/topics
GET  /api/documents/{id}
GET  /api/recommendations?user_id=...&limit=...
POST /api/profile/topics
POST /api/profile/feedback
```

单页 Web 包含：

- 已有知识主题选择；
- 新知识/深化知识推荐列表；
- Stack Exchange/arXiv 来源筛选；
- 关键词、分数构成和推荐理由；
- 数据规模、Spark 批次和许可证信息。

### 5.6 现有 Knowpipe 的复用边界

可复用：

- URL、文件、B 站和 Podcast/RSS 输入适配思路；
- HTML 清洗、文本切块和来源溯源经验；
- TF-IDF 候选召回的算法基线；
- 知识卡、四类知识差分和反馈闭环概念；
- 现有测试组织方式。

必须新增或重新实现：

- Spark DataFrame/MLlib 批处理；
- 万条文档的数据契约、质量统计和批次管理；
- MongoDB 存储、索引和幂等写入；
- 用户画像、推荐评分和评价；
- Flask API、Web 页面和端到端证据；
- Spec Kit 规约与课程交付材料。

不能直接作为课程证据：

- Python 线程池不能算 Spark；
- JSONL/SQLite 不能算 MongoDB；
- CLI/Markdown 不能算 Web；
- 当前少量本地知识卡不能算万条文档。

### 5.7 LLM 的辅助边界

LLM 可用于推荐理由润色、阅读提示、文档辅助和“可能冲突”候选生成。核心 TF-IDF、相似度、推荐分数、规模统计和 Spark 运行证据必须来自可复现程序。LLM 输出本身不能作为核心挖掘成果。

---

## 6. 基线、提升与惊喜范围

| 范围 | 功能 | 前置条件 | 验收证据 | 放弃条件 |
|---|---|---|---|---|
| 基线 | ≥10,000 条有效 Stack Exchange 文档及 arXiv 补充 | 数据源和许可获批 | 实采、去重、有效和入库数量 | 主源未获批则换教师数据集 |
| 基线 | Spark 清洗、TF-IDF、关键词和相似度 | 集群或获批 local 环境 | 日志、DAG、条数、耗时、样例 | 不允许 Spark 时必须调整选题 |
| 基线 | MongoDB 保存文档、结果和用户知识 | MongoDB 实例与权限 | 集合、索引、查询、入库统计 | 无实例时申请课程环境，SQLite 不冒充 |
| 基线 | Flask API 和单页 Web | Flask、MongoDB 可连接 | API JSON、页面、视频 | 图表库不可用则用原生 HTML |
| 基线 | 已知过滤、新知识和深化知识（关键词匹配 + KMeans 主题簇兜底） | 用户知识画像和 Spark 特征 | 推荐理由、分数、人工 top-k 评价 | 无行为数据则用显式主题和人工标注 |
| 基线 | SDD、Git、报告、提示词和演示 | Spec Kit、远程仓库 | 规约、提交、PPT、≤5 分钟视频 | 无远程时保留本地 bundle 并确认渠道 |
| 提升 | 跨来源主题关联 | 两源稳定入库 | 问答与论文关联样例及评价 | 影响基线或无法解释则取消 |
| 提升 | 新/已知/深化/可能冲突解释 | 基线分类稳定 | 分类规则、证据、人工复核 | 冲突误报高则取消冲突类 |
| 提升 | 主题簇质量评价与调优 | KMeans 基线完成 | Silhouette 或关键词人工准确率 | 聚类不可解释则仅保留关键词层 |
| 提升 | 少量 B 站或 RSS 内容 | 许可和获取稳定性确认 | 授权、采集日志、字段样例 | 任一条件不清即放弃 |
| 惊喜 | B 站/RSS 增量更新 | 基线稳定且有余量 | 增量批次、去重和恢复日志 | 第 10 天基线未稳则不做 |
| 惊喜 | 用户反馈校准画像 | 推荐与 Web 稳定 | 反馈前后排序变化 | 无评价样本则不做 |
| 惊喜 | 工程问题到论文的知识扩展 | 跨源映射质量达标 | 关联词、相似度和跳转链路 | 噪声高则取消 |
| 惊喜 | 丰富可视化 | API 和证据链完成 | 主题图、来源分布、评分拆解 | 不占用基线修复时间 |

---

## 7. 三周实施计划

### 7.1 第 1 周：确认条件、数据和规约

| 时间 | 任务与负责人 | 输入依赖 | 完成证据 | 风险与降级 |
|---|---|---|---|---|
| 第 1 天 | 陶文杰核验 Spark；陈铭宇核验 MongoDB、Web 和提交环境；共同建立 Spec Kit 需求草案 | 课程群和教师环境信息 | 版本、命令、地址、权限与未确认项清单 | 不安装未知依赖；缺信息当天询问 |
| 第 2 天 | 陶文杰验证集群最小作业或 local 可行性；陈铭宇验证 MongoDB 连接和 Flask 运行条件 | 环境权限 | Spark 应用结果或失败记录；MongoDB 连接结果 | 无集群则申请 local；无 MongoDB 则申请实例 |
| 第 2～3 天 | 陶文杰核验 Stack Exchange 获取方式和 100 条样本；陈铭宇核验许可并取得 arXiv 100 条样本 | 数据许可、网络 | 原始响应、字段样例、许可证表 | API 失败则尝试 Dump 或教师数据 |
| 第 3～4 天 | 共同确定统一 schema、去重规则、有效文档定义和规模估算 | 两源样本 | 数据字典、样本统计、失败率和规模估算 | 主源不能独立支撑万条则不通过闸门 |
| 第 4 天 | 陈铭宇整理需求规约和分工；陶文杰整理数据与环境证据；双方互审 | 前述证据 | 需求规约、数据说明、分工表 | 未确认项必须显式标记 |
| 第 5 天 | 共同完成选题确认，冻结基线与备用方案 | 教师反馈 | 确认记录、决策和变更清单 | 未确认通过前不进入正式开发 |

### 7.2 第 2 周：最小切片、万条扩展与 Web 联调

| 时间 | 任务与负责人 | 输入依赖 | 完成证据 | 风险与降级 |
|---|---|---|---|---|
| 第 6 天 | 陶文杰完成 100+100 条 Spark 清洗/TF-IDF；陈铭宇完成 MongoDB schema、索引和幂等导入 | 选题通过、环境可用 | Spark 日志、清洗对照、Mongo 查询 | Connector 不兼容则使用中间文件导入 |
| 第 7 天 | 陈铭宇完成文档/主题 API；陶文杰完成关键词和相似度；共同贯通 Spark→Mongo→API | 上一步输出 | API JSON、Mongo 结果、批次 ID | 先保留只读 API，不增加复杂权限 |
| 第 8 天 | 陈铭宇完成用户画像和单页 Web；陶文杰完成已知过滤和新/深化排序；共同完成垂直切片 | 端到端数据可查 | 页面、推荐理由、端到端录屏 | 图表失败则用原生页面 |
| 第 9 天 | 陶文杰扩大采集并运行万条 Spark 作业；陈铭宇完成 API 分页和页面联调 | 配额、存储容量 | 输入、有效、输出、入库统计 | 批量过慢则分批，不降低最终门槛 |
| 第 10 天 | 陈铭宇组织人工标注；陶文杰计算 Precision@K/NDCG 和非个性化基线；共同修复 | 稳定推荐 | 标注表、指标、基线对比 | 无真实行为时明确模拟画像和双人标注 |

### 7.3 第 3 周：评价、证据、文档与演示

| 时间 | 任务与负责人 | 输入依赖 | 完成证据 | 风险与降级 |
|---|---|---|---|---|
| 第 11 天 | 陶文杰复跑完整 Spark 作业；陈铭宇完成 Mongo/API/Web 稳定性测试 | 万条数据 | 最终日志、错误率、接口测试、索引说明 | 集群临时失败则保留结果并准备 local 演示 |
| 第 12 天 | 共同评估并最多加入一个提升功能 | 基线全部通过 | 跨源关联或解释功能和测试 | 任一基线未通过就取消亮点 |
| 第 13 天 | 陈铭宇主整理规约、提示词和报告；陶文杰补数据、算法和运行证据；交叉审阅 | 最终实现 | Spec Kit、提示词、报告草稿 | 文档必须与实现一致 |
| 第 14 天 | 陶文杰准备数据/Spark；陈铭宇准备个性化/Web/评价；共同录制 ≤5 分钟视频 | 稳定演示版 | PPT、视频、截图、演讲稿 | 准备离线数据与录屏 |
| 第 15 天 | 双人计时演练、互问模块、核对 Git 和提交包 | 全部交付物 | 8～10 分钟演练、最终分工、提交清单 | 不加新功能，只修阻断问题 |

Git 应按数据核验、Spark、MongoDB、API/Web、评价和交付物等里程碑分别提交，并关联规约或任务编号。

---

## 8. 陶文杰与陈铭宇初版分工

| 成员 | 模块/层次 | 具体任务 | 输入依赖 | 交付证据 | 预计占比 | 备份负责人 |
|---|---|---|---|---|---:|---|
| 陶文杰 | 数据源与 Spark 层 | Stack Exchange 采集、统一字段、清洗、去重、TF-IDF、关键词、万条任务和集群证据 | 数据许可、Spark 环境 | 数据统计、作业、日志、DAG、性能结果 | 30% | 陈铭宇 |
| 陶文杰 | 挖掘与评价 | 相似度、已知过滤、新/深化排序、基线对比和指标 | 用户画像、Mongo 结果 | 公式、指标、样例解释、测试 | 20% | 陈铭宇 |
| 陈铭宇 | MongoDB 与业务层 | 集合、索引、幂等写入、文档/主题/推荐 API | Spark 输出、MongoDB | schema、索引、查询、API 测试 | 30% | 陶文杰 |
| 陈铭宇 | 用户与交互层 | 用户知识画像、主题选择、推荐页面、规约和演示整合 | API、推荐结果、Spec Kit | 页面、截图、规约、视频 | 20% | 陶文杰 |

总贡献初定为陶文杰 50%、陈铭宇 50%。

协作规则：

- Spark 数据结构和 MongoDB schema 必须共同评审；
- 推荐评分、评价方法和四层架构属于共同设计决策；
- 陶文杰必须能够独立启动并解释 API/Web；
- 陈铭宇必须能够解释 Spark master、TF-IDF 和数据规模证据；
- 最终贡献按真实提交和交付调整，并说明与初版分工的差异。

---

## 9. 风险与决策闸门

| 风险 | 最晚确认时间 | 验证方法 | 失败替代 | 是否缩减范围 |
|---|---|---|---|---|
| Stack Exchange 许可与展示边界 | 第 3 天 | 官方许可、记录级许可证、教师意见 | 只展示必要片段和链接；必要时换主源 | 是 |
| 实际文档不足 1 万条 | 第 4 天初验；第 9 天终验 | 样本失败率和实际入库统计 | 增加采集余量或换获批数据 | 是，未达标不得宣称完成 |
| API 配额或 Dump 不可用 | 第 3 天 | 分页请求、配额、Dump 可访问性 | API Key、分日采集、教师数据 | 是 |
| 真实 Spark 集群不可用 | 第 2 天 | 最小应用、master、worker/executor | 经教师批准用 `local[*]` | 不减功能，但降低运行路径 |
| MongoDB 不可用 | 第 2 天 | 版本、连接、写入和查询 | 申请课程/远程实例或本机安装许可 | 不能用 SQLite 冒充 |
| arXiv 摘要是否算文档 | 第 5 天 | 教师确认 | 不计入万条，只作补充 | 否 |
| 个性化缺少真实行为 | 第 5 天定方法；第 10 天试评 | 多个画像、双人标注、top-k 指标 | 显式主题 + 人工相关性评价 | 否 |
| 冲突知识误报 | 第 10 天 | 人工复核、证据片段 | 改为“可能冲突”或取消 | 是 |
| B 站/RSS 许可或获取不稳定 | 第 10 天 | 授权、正文、重复运行 | 完全取消 | 是，不影响基线 |
| 答辩环境故障 | 第 14 天 | 离线、断网和重启测试 | 固定数据、截图、≤5 分钟视频 | 否 |

---

## 10. 8～10 分钟公开汇报结构

建议控制在约 9 分钟：

| 时间 | 内容 | 展示重点 |
|---|---|---|
| 0:00～0:50 | 背景与问题 | 内容分散、重复推荐、缺少知识差分 |
| 0:50～1:50 | 数据来源 | Stack Exchange、arXiv、规模与许可 |
| 1:50～3:00 | Spark 挖掘 | 清洗、TF-IDF、关键词、相似度 |
| 3:00～4:00 | 个性化机制 | 已有知识、已知过滤、新/深化知识 |
| 4:00～5:00 | 四层架构 | Spark→MongoDB→Flask→Web |
| 5:00～6:30 | 演示路径 | 选择主题、查看推荐、理由与来源 |
| 6:30～7:20 | 评价与证据 | 万条统计、运行模式、指标、查询 |
| 7:20～8:10 | 三周计划与分工 | 条件闸门、切片、扩展、两人各 50% |
| 8:10～9:00 | 风险与预期成果 | 集群优先、local 降级、可选功能可放弃 |

推荐现场演示路径：

```text
选择已有主题 Python、Spark
    ↓
查看新知识和深化知识推荐
    ↓
筛选工程问答或学术摘要
    ↓
展开关键词、分数和推荐理由
    ↓
查看许可证、来源链接和 Spark 批次证据
```

### 10.1 预期交付成果

- 不少于 10,000 条有效技术文档；
- 一套真实 Spark 批处理与挖掘作业；
- 一个 MongoDB 技术文档和用户知识库；
- 一组可运行的 Flask Web API；
- 一个可交互的个性化知识发现页面；
- 推荐评价、运行日志、截图和数据证据；
- Spec Kit 需求/设计规约、全部提示词、Git 历史、报告、PPT、分工表和备份视频。

---

## 11. 仍需教师或项目成员确认的问题

1. 教师是否批准“Stack Exchange + arXiv metadata”，并认可 arXiv 标题和摘要作为补充文档？
2. 真实 Spark 集群的版本、master、提交方式和资源配额是什么；若不可用，是否明确允许 `local[*]`？
3. MongoDB 实例、版本、账号权限和 Connector 条件是什么；是否允许在本机安装？
4. 教师是否接受“已有知识主题/知识卡 + 人工相关性标注 + Precision@K/NDCG”作为个性化及评价方案？
5. GitHub Spec Kit 模板、远程仓库、最终提交渠道、演示机器和具体截止时间是什么？

---

## 12. PPT 转换说明

本报告是详细审阅版本，不建议直接逐页投影。配套文件 [课程项目选题汇报.md](课程项目选题汇报.md) 不是对报告的机械格式转换，而是按照 8～10 分钟现场汇报重新组织叙事：一页只保留一个核心结论，长表格改为流程、对比卡片和时间线，详细依据移入演讲提示或备份材料，并使用 `---` 作为幻灯片分页符。

本机可通过以下命令生成 PPTX：

```bash
python3 build_course_presentation.py \
  --input 课程项目选题汇报.md \
  --output 课程项目选题汇报.pptx
```

如果后续安装 Marp CLI，也可以从同一 Markdown 幻灯片源进行转换。生成 PPT 后应人工检查字体、换行、投影比例和现场演示截图，再进行最终美化。
