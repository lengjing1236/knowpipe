# 知识精炼管道（Knowpipe）— 第一性原理设计文档

> 目标：输入任意内容（文章 / 网页 / 视频逐字稿 / 文本文件）→ 只输出**你还不知道的新知识**，
> 并沉淀为可主动回忆的原子知识卡，加速学习。
> 本期明确**不做操作层**（淘宝下单 / 上传同步等），聚焦"信息 → 新知识"的加工管道。

---

## 1. 人类已有的相关经验（研究结论）

动手前先搜集了这个领域前人的经验，它们分别回答"这个管道应该怎么设计"的不同子问题：

### 1.1 增量阅读（Incremental Reading，Piotr Wozniak / SuperMemo）
- 把整篇文章导入系统，边读边按重要度**抽取片段**，片段再转成**问答卡**，交给间隔重复算法排期复习。
- 关键思想：**知识不是一次性读完的，而是边读边抽、被拆成原子后反复加工**。"一次粗读，复习时细读"是被允许的。
- 对我们的启示：输出不该是"摘要"，而是可被进一步加工、复习的原子单元。

### 1.2 Zettelkasten 卡片盒（Niklas Luhmann /《卡片笔记写作法》）
- **原子笔记**：一条笔记 = 一个可独立成立的想法，不依附于"第几章的笔记"。
- **用自己的话写**：复述本身就是理解，"写不出来 = 还没懂"。
- **链接重于文件夹**：价值在笔记之间的连接网络，而非分类目录。
- 对我们的启示：知识卡必须原子化、必须用自己的话表达、必须带可链接的标识。

### 1.3 记忆科学（Ebbinghaus 遗忘曲线 / 间隔效应）
- 遗忘曲线 `R(t)=e^(−t/S)`，24 小时内约 2/3 的新内容会丢失；但**有意义的理解**遗忘远比无意义音节慢。
- 分散复习优于集中复习（间隔效应，已在数百项研究复现）；主动回忆优于重读。
- 对我们的启示：管道产出应**优先追求"理解"而非"记忆事实"**，并给出可做主动回忆的问答卡形态（而不是只给一段文字）。

### 1.4 AI 时代的记忆系统（iText2KG / AtomMem / PersonalAI / AntigravityRAG / AWS KnowledgeForge 等）
- 通用管道范式高度一致：**解析 → 切块 → 抽取原子知识 → 语义去重 → 图/库 → 矛盾与替代处理 → 遗忘衰减**。
- "写入前先读回"：生成新内容前先检索已有知识，只落未覆盖的部分（KnowledgeForge 的闭环）。
- 矛盾/替代：新事实覆盖旧事实时旧卡标记失效（valid_until），保留历史（版本控制）。
- 对我们的启示：**语义去重 + 矛盾处理 + 反馈闭环**是整个管道的心脏，不是可选项。

---

## 2. 第一性原理拆解

**第一性原理：** 学习 = 把外部信息转化为"你脑中未知且未理解"的知识原子，并让这些原子能被主动回忆。

从这个原理反推，管道必须满足五个条件：

| 编号 | 必要条件 | 推论出的设计决策 |
|---|---|---|
| P1 | 信息必须先**降维成原子**。文章/视频是容器不是知识，知识是可独立成立的最小断言 | 提取 → 清洗 → 分解成"原子知识卡" |
| P2 | "已知/未知"需要**关于你脑内状态的代理模型**。无法直接读脑，只能用外部记忆库记录"你已接触且确认理解"的知识 | 持久化记忆库（`cards.jsonl`）+ 来源溯源 |
| P3 | 去重必须是**语义级**而非字面级。同一概念换种说法依然相同 | 候选召回（向量粗筛）+ LLM 语义判定两段式 |
| P4 | "新"不是二值，而是有程度：全新 / 深化 / 修正 / 矛盾 | 四分类判定：`new / known / refine / contradict` |
| P5 | 过滤必须**可校准**且**防回音壁**。novelty 判定会漂移，需要反馈闭环；只推增量会丢失重遇价值与偶然发现 | `review` 评分反馈回路 + 报告保留"全量摘要"通道 |

---

## 3. 架构与数据流

```
信息源                    处理（pipeline）                          输出
─────────              ──────────────────────────              ─────────────
定时 RSS     ─┐
文章 URL     ─┤
本地文本/文件 ─┤→ [ingest]  → [chunk]  → [decompose]  → [classify] → [report]
视频逐字稿   ─┤   抓取/清洗   切块       LLM 抽原子卡    LLM 语义差分  新知识报告
Podcast RSS ──┘
B站 BV号    ───→ [bilibili] 官方字幕优先 → 无字幕走飞书妙记ASR  ─┐        + 问答卡建议
                         --bili-pages 1-43 → 循环每页进管道，共享记忆库跨P去重 ─┘
                         │                       │             │
                         ▼                       ▼             ▼
                 [memory: cards.jsonl]  ← 候选召回(向量粗筛)  ─┘
                          ▲
                          └── [review] 用户打分反馈 → 校准记忆库
```

自动化 Podcast watcher 位于输入适配器之上：它轮询一个或多个 RSS/Atom，使用
`feed_url + episode GUID` 作为稳定去重键，并在独立的 `state/podcast_watch.db`
中维护 `discovered → processing → success/failed` 状态。成功报告以临时文件原子
替换写入 `archive/podcasts/<节目名>/`，可通过 `--once` 配合 cron，或常驻模式按小时
轮询。Webhook/SMTP 通知是可选后端，默认不连接外部服务。

模块：
- **ingest**：URL 抓取 / 文本 / 文件 → 清洗为纯文本（可容纳视频逐字稿粘贴）。
- **bilibili**：BV 号 → 逐字稿。官方 AI 字幕优先（WBI 签名 + 可选 cookie）；无字幕自动降级到飞书妙记 ASR。支持 `--bili-pages 1-3/all` 合集批量：循环每个分P 各自进管道，共享同一记忆库做跨P去重；逐字稿磁盘缓存（`cache/bili_transcripts/`）避免重复 ASR。
- **podcast**：RSS/Atom feed → 选择 episode；优先读取节目提供的 transcript（含 `podcast:transcript` 扩展），无文字稿时下载音频并用本地 faster-whisper 转写，再进入同一条管道。
- **chunk**：长文本切块（LLM 上下文窗口友好）。
- **decompose**：把每块内容交给 LLM 抽成"原子知识卡"（自己的话、一句话、独立成立、带主题标签与确定性）。
- **classify**：每张新卡先对记忆库做**向量候选召回**（粗筛 top-k），再交 LLM 结合候选做四分类语义判定。
- **memory**：原子卡持久化（默认 JSONL，也支持 `.db/.sqlite` 的事务式 SQLite），带状态机与溯源；写入前先检索（防重复）。
- **report**：输出新知识摘要（按主题聚类）+ 问答卡建议 + 需裁决的冲突 + 已跳过清单（供抽查过滤质量）+ 全量摘要（防回音壁）。文章级视频报告只保留整理后的知识文章，不输出 ASR 原稿或中间清洗稿。
- **review**：用户对新卡评分（"我早知道了" / "学到新东西"），反馈回写记忆库，校准后续过滤。
- **ask**：从个人记忆库召回相关卡片后回答问题；OpenAI 模式严格基于候选卡并附卡片 ID，离线模式返回可追溯的候选清单。
- **watch**：定时轮询多 Podcast feed，按 GUID 去重，调用同一条 pipeline，原子归档
  Markdown 报告，并按配置发送 webhook/SMTP 通知。

### 记忆卡 schema
```json
{
  "id": "c_<sha1 前12位>",
  "claim": "可独立成立的原子断言（自己的话）",
  "source": "来源标识",
  "topic": "主题标签",
  "certainty": "fact | principle | opinion",
  "status": "new | known | learned | refined | conflict",
  "relates_to": "关联的记忆卡 id（refine/conflict 时）",
  "seen_count": 0,
  "created": "ISO 时间", "last_seen": "ISO 时间"
}
```

---

## 4. MVP 边界

### 本期做
- URL / 文本 / 本地文件 / Podcast RSS 四种摄入；长文本自动切块。
- **B站通道**：`--bilibili BV` 一条命令 → 官方字幕或飞书妙记 ASR → 逐字稿 → 管道。
- **B站合集批量**：`--bili-pages 1-3/all` 循环分P，共享记忆库跨P去重，逐字稿缓存，输出含逐P统计的汇总报告；加 `--bili-resume` 后每个成功分P都会写检查点，任务中断可从未完成分P继续。
- 原子知识卡分解（LLM）；语义级四分类差分（候选召回 + LLM 判定）。
- 记忆库持久化、写入前检索、冲突标记、review 反馈回写。
- Markdown 新知识报告（含问答卡建议、跳过清单、全量摘要）。
- 综合模式同时输出长文总结与知识差分卡；`ask` 命令可基于个人记忆库检索问答。
- JSONL→SQLite 迁移、合集断点检查点、LLM 重试与结构校验。
- Podcast 自动轮询、episode 状态 SQLite、报告归档与可选 webhook/SMTP 通知。
- 大脑（Brain）三层可切换：`openai`（真实 LLM API）/ `manual`（注入人工/Agent 判定，用于演示与复现）/ `heuristic`（零依赖离线兜底，开箱即跑）。

### 本期不做（下期再议）
- **间隔重复调度器**：本期只产出问答卡建议，不排期（复用 SuperMemo/SM-2 是自然下一步）。
- **知识图谱 / 多跳推理**：本期用"标签 + 关联 id"近似。
- **增量更新**：跨来源的细粒度内容更新仍未做；watch 已支持按 episode GUID 去重和失败重试。
- **并行处理**：多P 同时下载+ASR（目前仍串行，43P 需较长时间）。
- **本地 whisper 常驻**：官方字幕与妙记都不可用时的离线 ASR 兜底，本期仅留接口。
- **操作层**：用户已明确本期去掉。

---

## 5. 验证方式
- 用一份与"种子记忆"部分重叠的真实文章端到端跑通：验证 (a) 已知内容被过滤、(b) 新内容被抽出并归类、(c) 记忆库正确更新、(d) 报告结构完整、(e) review 反馈能回写。
- 三个 Brain provider 各跑一遍，确认 `heuristic` 零依赖可跑、`manual` 能复现语义判定。

## 6. 个人使用场景的演进路线

当前目标是个人本地知识工作台，而不是多用户服务：

1. 本地保存缓存、记忆库和任务检查点；云端 Brain 使用 OpenAI 兼容接口（例如 `chat.ekti.cc` 的 GPT-5.6）。
2. 输入适配器保持可插拔；目前支持 URL、文件、B站和 Podcast RSS，Podcast 优先读取 transcript、再回退本地 Whisper。
3. 同一份输入可选择长文总结、未知知识卡，或 `integrated` 综合报告；`ask` 只基于个人记忆库候选卡回答并引用卡片 ID。
4. 记忆库默认兼容 JSONL，规模变大时使用 SQLite；两者通过同一套 Store 接口接入流水线。

下一阶段优先级：混合关键词/embedding 检索、来源时间戳证据、可恢复的跨来源任务历史，以及针对新知识过滤和总结质量的固定评测集。

## 7. 下一阶段架构路线（个人版）

当前实现已经覆盖 RSS watcher、长文/卡片/问答三类核心工作流。若继续增强，建议把
“命令行脚本”演进为一个可恢复的本地知识工作台，而不是继续在 `cli.py` 中堆积来源
分支。

### 7.1 目标分层

```text
[SourceAdapter] RSS / URL / B站 / Podcast / PDF
        ↓
[Ingest + Artifact] 原文、音频、transcript（私有缓存，带 hash）
        ↓
[Job Ledger] ingest → transcribe → extract → retrieve → synthesize → persist → notify
        ↓
[Knowledge Store] cards + provenance + relations + FTS/embedding 索引
        ↓
[Renderer] 长文总结 / 未知知识卡 / 记忆库问答 / 通知
```

每个阶段都应有稳定的 `run_id`、输入 hash、prompt/model 版本、attempt 次数和错误
信息；阶段成功后才推进状态。这样网络中断、LLM 限流或进程崩溃只重跑失败阶段，
不会重新下载音频或重复调用已经完成的 LLM 请求。

### 7.2 最值得优先做的四项

1. **分层长文总结**：当前文章级路径将 transcript 截到约 20,000 字后单次调用；对长
   Podcast 会漏掉中后段内容。改为“分块提炼 → 全局提纲 → 统一成文 → 覆盖率/矛盾
   检查”，只在短内容时走单调用，以兼顾 GPT-5.6 的质量和速度。
2. **证据与时间戳**：知识卡增加 `source_id`、`segment_id`、`start/end`、语言和
   speaker（可为空）。报告不输出完整 ASR 稿，但每条卡和总结可跳回 Podcast 时间点；
   这会同时提升问答可信度和冲突裁决能力。
3. **真正的混合检索**：SQLite FTS5/BM25 负责关键词召回，embedding 负责语义召回，
   再由轻量 reranker 合并；当前 TF-IDF/可选 embedding 已是基础，但 embedding 尚未
   成为持久化索引，也没有按主题、状态、时间做过滤。
4. **评测与反馈闭环**：保存一组“已知/未知/深化/冲突”的黄金样例，固定评估卡片
   novelty precision、摘要覆盖率、引用正确率和问答可追溯率。`review` 的结果既要
   校准记忆库，也要进入评测集，避免提示词改动后质量悄悄回退。

### 7.3 数据模型建议

将当前分离的 episode 状态和记忆卡统一关联到四类实体：

- `source`：来源类型、URL、外部 ID（Podcast GUID）、标题和订阅策略；
- `artifact`：原文/音频/transcript/摘要/报告，content hash、语言、版本和本地路径；
- `job`：阶段状态、重试次数、错误、模型和 prompt 版本；
- `card`：断言、状态、时间有效性、主题/实体、`relates_to`，以及一到多个证据片段。

Whisper 音频默认在转写完成后立即删除；transcript 只留在本地短期缓存（watch 默认
30 天 TTL），报告只保留总结、知识差分和可选的短证据引用。
这样既满足“不输出语音识别文稿”，又不会牺牲可验证性。

### 7.4 分阶段落地

1. **可靠性层**：Job Ledger、统一 `SourceAdapter` 接口、SQLite FTS5、证据片段和
   `watch status/retry`；保持现有 CLI 不变。
2. **质量层**：分层总结、术语/实体标准化、时间有效性与冲突图、黄金评测集。
3. **体验层**：本地只读 Web 仪表盘（订阅源、失败任务、报告和问答），手机通知继续
   作为入口；未来新增 Newsletter、PDF、网页收藏时只需增加 Adapter。

默认建议是：先做第 1 阶段，继续使用本地 SQLite + cron + 云端 GPT-5.6；暂不引入
多人服务、Anki/Obsidian 同步或公开 Web 服务。
